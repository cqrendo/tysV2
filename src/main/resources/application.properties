server.compression.enabled=true
server.compression.mime-types=application/json,application/xml,text/html,text/xml,text/plain,application/javascript,text/css
security.basic.enabled=false
server.tomcat.uri-encoding=UTF-8
spring.jackson.serialization.write_dates_as_timestamps=false
# Comment out if using anything else than H2 (e.g. MySQL or PostgreSQL)
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Uncomment if using PostgreSQL
#spring.jpa.hibernate.ddl-auto=create
#spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
#spring.datasource.url=jdbc:postgresql://localhost:5432/bakery_flow_spring
#spring.datasource.username=springuser
#spring.datasource.password=ThePassword

logging.level.org.atmosphere = warn

# Ensure application is run in Vaadin 14/npm mode
vaadin.compatibilityMode = false
ldap.enabled = true
ldap.urls=ldap://intergal01.cloud.netimaging.net:389
ldap.base.dn= dc=intergal,dc=coop
ldap.username= cn=admin,dc=intergal,dc=coop
ldap.password= toorLDAP44
ldap.user.dn.pattern = uid={0}
spring.main.banner-mode=off
spring.thymeleaf.cache=false

